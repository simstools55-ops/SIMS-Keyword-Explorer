# SIMS Keyword Explorer v0.3.4 Release Notes

This patch does not change the cannibalization decision itself.
It fixes two user-facing inconsistencies found during real operation:
an unrelated nearest URL remaining on a no-owner candidate, and an ambiguous
"existing update" label that looked like an existing-article rewrite instruction.
