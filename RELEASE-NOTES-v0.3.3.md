# SIMS Keyword Explorer v0.3.3 Release Notes

This patch fixes a critical false-positive cannibalization case discovered in the
first real External Discovery test.

A new topic can no longer be redirected to an unrelated existing article merely
because both titles use generic words such as "使い方" or "新機能".
